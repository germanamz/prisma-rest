name: Checks

on:
  pull_request:
    branches:
      - main
  workflow_call:

jobs:
  lint:
    runs-on: ubuntu-latest
    name: Lint
    steps:
      - uses: ./.github/actions/setup

      - name: Run lint
        run: pnpm run lint

  typecheck:
    runs-on: ubuntu-latest
    name: Typecheck
    needs:
      - lint
    steps:
      - uses: ./.github/actions/setup

      - name: Build packages
        run: pnpm run build

      - name: Generate bins
        run: pnpm install

      - name: Run typecheck
        run: pnpm run typecheck
